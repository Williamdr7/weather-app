import { FlexContainer } from "@/components/ui";
import styled from "styled-components";

export const MainWeatherInfoContainer = styled(FlexContainer)`
  width: 100%;
  border-radius: 24px;
`;

export const WeatherIcon = styled.img`
  transform: scale(1.5);
`;
